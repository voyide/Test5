name: prograde_test_taker
description: Professional-grade test taking app with JSON import, analytics, categories, and quiz workflows.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
